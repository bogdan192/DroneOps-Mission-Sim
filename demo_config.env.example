# Optional local demo configuration.
# Copy this file to demo_config.env and paste a browser Google Maps key if you
# want the Google Maps view instead of the built-in fallback map.
#
# GOOGLE_MAPS_API_KEY=<API_KEY>
